# 快递比价系统 - 完整架构设计文档

## 📋 目录

1. [项目概述](#项目概述)
2. [技术栈选型](#技术栈选型)
3. [系统架构设计](#系统架构设计)
4. [项目目录结构](#项目目录结构)
5. [前端设计](#前端设计)
6. [后端设计](#后端设计)
7. [快递公司模块设计](#快递公司模块设计)
8. [OpenClaw集成设计](#openclaw集成设计)
9. [数据库设计](#数据库设计)
10. [错误处理与日志](#错误处理与日志)
11. [性能优化策略](#性能优化策略)
12. [安全设计](#安全设计)
13. [测试方案](#测试方案)
14. [部署方案](#部署方案)
15. [开发计划](#开发计划)

---

## 项目概述

### 1.1 项目背景

开发一个快递比价网页应用，帮助用户快速比较不同快递公司的价格，选择最优惠的寄件方案。

### 1.2 核心功能

- **价格查询**：用户输入寄件信息，实时获取多家快递公司报价
- **智能排序**：按价格从低到高排序，突出显示最低价
- **详细信息**：展示预计时效、服务类型等详细信息
- **历史记录**：保存用户查询历史（可选扩展）

### 1.3 支持的快递公司

| 快递公司 | 数据获取方式 | 优先级 |
|---------|------------|-------|
| 顺丰速运 | OpenClaw爬取 | 高 |
| 中通快递 | OpenClaw爬取 | 高 |
| 圆通速递 | OpenClaw爬取 | 高 |
| 韵达快递 | OpenClaw爬取 | 高 |
| 申通快递 | OpenClaw爬取 | 高 |
| 极兔速递 | OpenClaw爬取 | 中 |
| 邮政EMS | OpenClaw爬取 | 中 |
| 德邦快递 | OpenClaw爬取 | 中 |

---

## 技术栈选型

### 2.1 前端技术栈

```
- 框架：Vue 3 + Vite
- UI组件库：Element Plus
- HTTP客户端：Axios
- 状态管理：Pinia
- 路由：Vue Router
- 样式：SCSS + CSS Variables
- 图标：@iconify/vue
- 构建工具：Vite
```

### 2.2 后端技术栈

```
- 运行时：Node.js 18+
- 框架：Express.js / Koa.js
- 爬虫工具：OpenClaw（基于Puppeteer）
- HTTP客户端：Axios
- 缓存：Node-cache（内存缓存）/ Redis（可选）
- 日志：Winston
- 参数验证：Joi / Zod
- 进程管理：PM2
```

### 2.3 开发工具

```
- 代码规范：ESLint + Prettier
- 版本控制：Git
- API测试：Postman / Apifox
- 包管理器：pnpm
```

---

## 系统架构设计

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户浏览器                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              Vue 3 前端应用                          │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐         │   │
│  │  │ 表单组件  │  │ 结果展示  │  │ 历史记录  │         │   │
│  │  └──────────┘  └──────────┘  └──────────┘         │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP/HTTPS
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      后端服务层                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              Express/Koa 服务器                      │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐         │   │
│  │  │ 路由层    │  │ 控制器层  │  │ 服务层    │         │   │
│  │  └──────────┘  └──────────┘  └──────────┘         │   │
│  └─────────────────────────────────────────────────────┘   │
│                              │                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              快递模块适配层                          │   │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐      │   │
│  │  │ 顺丰   │ │ 中通   │ │ 圆通   │ │ 韵达   │ ...  │   │
│  │  └────────┘ └────────┘ └────────┘ └────────┘      │   │
│  └─────────────────────────────────────────────────────┘   │
│                              │                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              OpenClaw 爬虫引擎                       │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐         │   │
│  │  │ 浏览器池  │  │ 任务队列  │  │ 结果缓存  │         │   │
│  │  └──────────┘  └──────────┘  └──────────┘         │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    快递公司官网/接口                         │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐              │
│  │ 顺丰   │ │ 中通   │ │ 圆通   │ │ 韵达   │ ...          │
│  └────────┘ └────────┘ └────────┘ └────────┘              │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 数据流程图

```
用户输入 → 前端验证 → API请求 → 后端接收 → 参数校验
    ↓
并行调用各快递模块 → OpenClaw爬取/接口调用 → 结果聚合
    ↓
价格排序 → 数据格式化 → 返回前端 → 结果展示
```

---

## 项目目录结构

### 4.1 完整目录结构

```
kuaibi/
├── frontend/                    # 前端项目
│   ├── public/
│   │   └── favicon.ico
│   ├── src/
│   │   ├── api/                # API接口
│   │   │   ├── index.js        # API统一导出
│   │   │   └── courier.js      # 快递相关API
│   │   ├── assets/             # 静态资源
│   │   │   ├── images/
│   │   │   └── styles/
│   │   │       ├── variables.scss
│   │   │       ├── reset.scss
│   │   │       └── global.scss
│   │   ├── components/         # 组件
│   │   │   ├── common/         # 通用组件
│   │   │   │   ├── Loading.vue
│   │   │   │   └── ErrorTip.vue
│   │   │   ├── courier/        # 快递相关组件
│   │   │   │   ├── SearchForm.vue      # 查询表单
│   │   │   │   ├── PriceCard.vue       # 价格卡片
│   │   │   │   ├── PriceList.vue       # 价格列表
│   │   │   │   └── CourierLogo.vue     # 快递logo
│   │   │   └── layout/         # 布局组件
│   │   │       ├── Header.vue
│   │   │       └── Footer.vue
│   │   ├── composables/        # 组合式函数
│   │   │   ├── useCourier.js
│   │   │   └── useLoading.js
│   │   ├── router/             # 路由
│   │   │   └── index.js
│   │   ├── stores/             # 状态管理
│   │   │   └── courier.js
│   │   ├── utils/              # 工具函数
│   │   │   ├── request.js      # Axios封装
│   │   │   ├── format.js       # 格式化工具
│   │   │   └── validate.js     # 验证工具
│   │   ├── views/              # 页面
│   │   │   ├── Home.vue        # 首页
│   │   │   └── Result.vue      # 结果页
│   │   ├── App.vue
│   │   └── main.js
│   ├── index.html
│   ├── vite.config.js
│   ├── package.json
│   └── .env.development
│
├── backend/                     # 后端项目
│   ├── src/
│   │   ├── config/             # 配置文件
│   │   │   ├── index.js        # 主配置
│   │   │   ├── courier.js      # 快递配置
│   │   │   └── openclaw.js     # OpenClaw配置
│   │   ├── controllers/        # 控制器
│   │   │   └── courierController.js
│   │   ├── middlewares/        # 中间件
│   │   │   ├── errorHandler.js
│   │   │   ├── rateLimit.js
│   │   │   └── validator.js
│   │   ├── models/             # 数据模型（可选）
│   │   ├── routes/             # 路由
│   │   │   ├── index.js
│   │   │   └── courier.js
│   │   ├── services/           # 服务层
│   │   │   ├── courierService.js
│   │   │   └── cacheService.js
│   │   ├── crawlers/           # 爬虫模块
│   │   │   ├── base.js         # 基类
│   │   │   ├── index.js        # 统一导出
│   │   │   ├── sf.js           # 顺丰
│   │   │   ├── zt.js           # 中通
│   │   │   ├── yt.js           # 圆通
│   │   │   ├── yd.js           # 韵达
│   │   │   ├── st.js           # 申通
│   │   │   ├── jt.js           # 极兔
│   │   │   ├── ems.js          # 邮政EMS
│   │   │   └── db.js           # 德邦
│   │   ├── utils/              # 工具函数
│   │   │   ├── logger.js
│   │   │   ├── helper.js
│   │   │   └── constants.js
│   │   └── app.js              # 应用入口
│   ├── logs/                   # 日志目录
│   ├── package.json
│   └── .env
│
├── docs/                        # 文档
│   ├── API.md
│   └── DEPLOYMENT.md
│
└── README.md
```

---

## 前端设计

### 5.1 页面设计

#### 5.1.1 首页（Home.vue）

**功能模块：**

1. **头部区域**
   - Logo + 标题
   - 简短介绍文案

2. **查询表单区域**
   - 寄件地选择（省市区三级联动）
   - 收件地选择（省市区三级联动）
   - 重量输入（kg，支持小数）
   - 物品类型选择（可选）
   - 查询按钮

3. **特性展示区域**
   - 快速比价
   - 多家对比
   - 实时准确

4. **底部区域**
   - 版权信息
   - 友情链接

**UI设计要点：**

```
- 配色方案：
  主色：#409EFF（蓝色）
  辅助色：#67C23A（绿色-最低价标识）
  警告色：#E6A23C（橙色）
  背景色：#F5F7FA
  
- 响应式设计：
  桌面端：居中布局，最大宽度1200px
  移动端：全宽布局，表单垂直排列
  
- 交互效果：
  表单输入实时验证
  查询按钮加载状态
  结果区域淡入动画
```

#### 5.1.2 结果页（Result.vue）

**功能模块：**

1. **查询条件展示**
   - 显示当前查询的寄件地、收件地、重量
   - 重新查询按钮

2. **结果列表区域**
   - 价格卡片列表（按价格排序）
   - 最低价标识
   - 加载状态
   - 空状态/错误状态

3. **价格卡片信息**
   - 快递公司Logo
   - 快递公司名称
   - 价格（突出显示）
   - 预计时效
   - 服务类型
   - 备注/说明

### 5.2 组件设计

#### 5.2.1 SearchForm.vue

```vue
<template>
  <div class="search-form">
    <el-form :model="form" :rules="rules" ref="formRef">
      <!-- 寄件地 -->
      <el-form-item label="寄件地" prop="senderAddress">
        <el-cascader 
          v-model="form.senderAddress"
          :options="addressOptions"
          placeholder="请选择寄件地"
        />
      </el-form-item>
      
      <!-- 收件地 -->
      <el-form-item label="收件地" prop="receiverAddress">
        <el-cascader 
          v-model="form.receiverAddress"
          :options="addressOptions"
          placeholder="请选择收件地"
        />
      </el-form-item>
      
      <!-- 重量 -->
      <el-form-item label="重量" prop="weight">
        <el-input-number 
          v-model="form.weight"
          :min="0.1"
          :max="100"
          :precision="1"
          placeholder="请输入重量"
        />
        <span class="unit">kg</span>
      </el-form-item>
      
      <!-- 查询按钮 -->
      <el-form-item>
        <el-button 
          type="primary" 
          @click="handleSubmit"
          :loading="loading"
        >
          立即比价
        </el-button>
      </el-form-item>
    </el-form>
  </div>
</template>
```

**数据结构：**

```javascript
const form = {
  senderAddress: [],    // [省, 市, 区]
  receiverAddress: [],  // [省, 市, 区]
  weight: 1             // kg
}
```

#### 5.2.2 PriceCard.vue

```vue
<template>
  <div class="price-card" :class="{ 'lowest': isLowest }">
    <div v-if="isLowest" class="lowest-badge">最低价</div>
    <div class="courier-info">
      <img :src="courierLogo" :alt="courierName" class="logo" />
      <span class="name">{{ courierName }}</span>
    </div>
    <div class="price-info">
      <span class="price">¥{{ price }}</span>
      <span class="unit">元</span>
    </div>
    <div class="detail-info">
      <div class="time">
        <el-icon><Clock /></el-icon>
        <span>{{ estimatedTime }}</span>
      </div>
      <div class="service">{{ serviceType }}</div>
    </div>
  </div>
</template>
```

### 5.3 API接口调用

#### 5.3.1 courier.js

```javascript
import request from '@/utils/request'

export const courierApi = {
  compare(data) {
    return request({
      url: '/api/compare',
      method: 'post',
      data
    })
  },
  
  getAddressOptions() {
    return request({
      url: '/api/address/options',
      method: 'get'
    })
  }
}
```

#### 5.3.2 request.js（Axios封装）

```javascript
import axios from 'axios'
import { ElMessage } from 'element-plus'

const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 30000
})

request.interceptors.request.use(
  config => {
    return config
  },
  error => {
    return Promise.reject(error)
  }
)

request.interceptors.response.use(
  response => {
    const res = response.data
    if (res.code !== 200) {
      ElMessage.error(res.message || '请求失败')
      return Promise.reject(new Error(res.message || '请求失败'))
    }
    return res.data
  },
  error => {
    ElMessage.error(error.message || '网络错误')
    return Promise.reject(error)
  }
)

export default request
```

---

## 后端设计

### 6.1 API接口设计

#### 6.1.1 接口列表

| 接口 | 方法 | 描述 |
|-----|------|------|
| /api/compare | POST | 快递比价 |
| /api/address/options | GET | 获取地址选项 |
| /api/health | GET | 健康检查 |

#### 6.1.2 /api/compare 接口详细设计

**请求参数：**

```json
{
  "senderProvince": "浙江省",
  "senderCity": "杭州市",
  "senderDistrict": "西湖区",
  "receiverProvince": "北京市",
  "receiverCity": "北京市",
  "receiverDistrict": "朝阳区",
  "weight": 1.5
}
```

**参数说明：**

| 参数 | 类型 | 必填 | 说明 |
|-----|------|------|------|
| senderProvince | string | 是 | 寄件省份 |
| senderCity | string | 是 | 寄件城市 |
| senderDistrict | string | 否 | 寄件区县 |
| receiverProvince | string | 是 | 收件省份 |
| receiverCity | string | 是 | 收件城市 |
| receiverDistrict | string | 否 | 收件区县 |
| weight | number | 是 | 重量(kg)，范围0.1-100 |

**响应格式：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "queryId": "q_20240101_123456",
    "queryTime": "2024-01-01 12:00:00",
    "results": [
      {
        "courierCode": "zt",
        "courierName": "中通快递",
        "courierLogo": "https://xxx.com/zt.png",
        "price": 12.00,
        "originalPrice": 15.00,
        "discount": "8折",
        "estimatedTime": "2-3天",
        "serviceType": "标准快递",
        "isLowest": true,
        "remark": ""
      },
      {
        "courierCode": "yt",
        "courierName": "圆通速递",
        "courierLogo": "https://xxx.com/yt.png",
        "price": 13.00,
        "originalPrice": 13.00,
        "discount": "",
        "estimatedTime": "2-3天",
        "serviceType": "标准快递",
        "isLowest": false,
        "remark": ""
      }
    ],
    "total": 2,
    "successCount": 2,
    "failCount": 0
  }
}
```

**响应字段说明：**

| 字段 | 类型 | 说明 |
|-----|------|------|
| queryId | string | 查询ID，用于追踪 |
| queryTime | string | 查询时间 |
| results | array | 结果数组，按price升序排列 |
| courierCode | string | 快递公司编码 |
| courierName | string | 快递公司名称 |
| courierLogo | string | 快递公司Logo URL |
| price | number | 最终价格 |
| originalPrice | number | 原价 |
| discount | string | 折扣信息 |
| estimatedTime | string | 预计时效 |
| serviceType | string | 服务类型 |
| isLowest | boolean | 是否最低价 |
| remark | string | 备注信息 |

**错误响应：**

```json
{
  "code": 400,
  "message": "参数错误：重量必须大于0",
  "data": null
}
```

**错误码定义：**

| 错误码 | 说明 |
|-------|------|
| 200 | 成功 |
| 400 | 参数错误 |
| 429 | 请求过于频繁 |
| 500 | 服务器内部错误 |
| 503 | 服务暂时不可用 |

### 6.2 路由设计

#### 6.2.1 routes/courier.js

```javascript
const express = require('express')
const router = express.Router()
const courierController = require('../controllers/courierController')
const { validateCompare } = require('../middlewares/validator')
const rateLimit = require('../middlewares/rateLimit')

router.post('/compare', 
  rateLimit({ windowMs: 60000, max: 10 }),
  validateCompare, 
  courierController.compare
)

router.get('/address/options', courierController.getAddressOptions)

module.exports = router
```

### 6.3 控制器设计

#### 6.3.1 controllers/courierController.js

```javascript
const courierService = require('../services/courierService')
const logger = require('../utils/logger')

class CourierController {
  async compare(req, res, next) {
    try {
      const params = req.body
      logger.info('比价请求', { params })
      
      const result = await courierService.comparePrices(params)
      
      res.json({
        code: 200,
        message: 'success',
        data: result
      })
    } catch (error) {
      logger.error('比价失败', { error: error.message })
      next(error)
    }
  }
  
  async getAddressOptions(req, res, next) {
    try {
      const options = await courierService.getAddressOptions()
      res.json({
        code: 200,
        message: 'success',
        data: options
      })
    } catch (error) {
      next(error)
    }
  }
}

module.exports = new CourierController()
```

### 6.4 服务层设计

#### 6.4.1 services/courierService.js

```javascript
const crawlers = require('../crawlers')
const cacheService = require('./cacheService')
const logger = require('../utils/logger')
const { COURIER_CONFIG } = require('../config/courier')

class CourierService {
  constructor() {
    this.crawlers = crawlers
  }
  
  async comparePrices(params) {
    const { senderProvince, senderCity, senderDistrict,
            receiverProvince, receiverCity, receiverDistrict, weight } = params
    
    const queryId = this.generateQueryId()
    const queryTime = new Date().toISOString()
    
    const cacheKey = this.getCacheKey(params)
    const cached = await cacheService.get(cacheKey)
    
    if (cached) {
      logger.info('使用缓存数据', { queryId, cacheKey })
      return {
        queryId,
        queryTime,
        ...cached,
        fromCache: true
      }
    }
    
    const promises = Object.entries(this.crawlers).map(([code, crawler]) => {
      return this.fetchPrice(crawler, code, params)
    })
    
    const results = await Promise.allSettled(promises)
    
    const successResults = results
      .filter(r => r.status === 'fulfilled' && r.value)
      .map(r => r.value)
      .sort((a, b) => a.price - b.price)
    
    if (successResults.length > 0) {
      successResults[0].isLowest = true
    }
    
    const response = {
      results: successResults,
      total: successResults.length,
      successCount: successResults.length,
      failCount: results.length - successResults.length
    }
    
    await cacheService.set(cacheKey, response, 300)
    
    return {
      queryId,
      queryTime,
      ...response,
      fromCache: false
    }
  }
  
  async fetchPrice(crawler, code, params) {
    try {
      const timeout = COURIER_CONFIG[code]?.timeout || 10000
      
      const result = await Promise.race([
        crawler.getPrice(params),
        this.timeout(timeout)
      ])
      
      return {
        courierCode: code,
        courierName: COURIER_CONFIG[code].name,
        courierLogo: COURIER_CONFIG[code].logo,
        ...result,
        isLowest: false
      }
    } catch (error) {
      logger.warn(`爬取${code}失败`, { error: error.message })
      return null
    }
  }
  
  timeout(ms) {
    return new Promise((_, reject) => {
      setTimeout(() => reject(new Error('请求超时')), ms)
    })
  }
  
  generateQueryId() {
    return `q_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`
  }
  
  getCacheKey(params) {
    const { senderProvince, senderCity, receiverProvince, receiverCity, weight } = params
    return `price:${senderProvince}:${senderCity}:${receiverProvince}:${receiverCity}:${weight}`
  }
  
  async getAddressOptions() {
    return require('../data/address.json')
  }
}

module.exports = new CourierService()
```

---

## 快递公司模块设计

### 7.1 基类设计

#### 7.1.1 crawlers/base.js

```javascript
class BaseCrawler {
  constructor(config) {
    this.config = config
    this.name = config.name
    this.code = config.code
  }
  
  async getPrice(params) {
    throw new Error('子类必须实现 getPrice 方法')
  }
  
  formatResult(data) {
    return {
      price: 0,
      originalPrice: 0,
      discount: '',
      estimatedTime: '',
      serviceType: '',
      remark: ''
    }
  }
  
  handleError(error) {
    console.error(`[${this.name}] 爬取失败:`, error.message)
    return null
  }
}

module.exports = BaseCrawler
```

### 7.2 统一返回格式

```javascript
{
  price: number,        // 最终价格（必填）
  originalPrice: number, // 原价（必填）
  discount: string,      // 折扣信息（可选）
  estimatedTime: string, // 预计时效（必填）
  serviceType: string,   // 服务类型（必填）
  remark: string         // 备注信息（可选）
}
```

### 7.3 顺丰速运模块示例

#### 7.3.1 crawlers/sf.js

```javascript
const BaseCrawler = require('./base')
const { openclaw } = require('../utils/openclaw')
const logger = require('../utils/logger')

class SFCrawler extends BaseCrawler {
  constructor() {
    super({
      name: '顺丰速运',
      code: 'sf'
    })
    this.url = 'https://www.sf-express.com/sf-service-owf-web/service/rate/mainland/query'
  }
  
  async getPrice(params) {
    const { senderProvince, senderCity, senderDistrict,
            receiverProvince, receiverCity, receiverDistrict, weight } = params
    
    try {
      const result = await openclaw.fetch({
        url: this.url,
        method: 'POST',
        headers: {
          'Content-Type': 'application/json'
        },
        data: {
          originProvince: senderProvince,
          originCity: senderCity,
          originDistrict: senderDistrict || '',
          destProvince: receiverProvince,
          destCity: receiverCity,
          destDistrict: receiverDistrict || '',
          weight: weight
        },
        timeout: 10000,
        waitFor: '.price-result'
      })
      
      return this.formatResult(result)
    } catch (error) {
      logger.error(`[${this.name}] 获取价格失败`, { error: error.message })
      throw error
    }
  }
  
  formatResult(data) {
    return {
      price: parseFloat(data.price) || 0,
      originalPrice: parseFloat(data.originalPrice) || 0,
      discount: data.discount || '',
      estimatedTime: data.estimatedTime || '1-2天',
      serviceType: data.serviceType || '顺丰标快',
      remark: data.remark || ''
    }
  }
}

module.exports = new SFCrawler()
```

### 7.4 中通快递模块示例

#### 7.4.1 crawlers/zt.js

```javascript
const BaseCrawler = require('./base')
const { openclaw } = require('../utils/openclaw')
const logger = require('../utils/logger')

class ZTCrawler extends BaseCrawler {
  constructor() {
    super({
      name: '中通快递',
      code: 'zt'
    })
    this.url = 'https://japi.zto.com/zto/api_utf8/chargeStandard'
  }
  
  async getPrice(params) {
    const { senderProvince, senderCity, 
            receiverProvince, receiverCity, weight } = params
    
    try {
      const result = await openclaw.fetch({
        url: this.url,
        method: 'POST',
        data: {
          sendProvince: senderProvince,
          sendCity: senderCity,
          receiveProvince: receiverProvince,
          receiveCity: receiverCity,
          weight: weight
        },
        timeout: 10000
      })
      
      return this.formatResult(result)
    } catch (error) {
      logger.error(`[${this.name}] 获取价格失败`, { error: error.message })
      throw error
    }
  }
  
  formatResult(data) {
    return {
      price: parseFloat(data.totalFee) || 0,
      originalPrice: parseFloat(data.originalFee) || 0,
      discount: '',
      estimatedTime: '2-3天',
      serviceType: '标准快递',
      remark: ''
    }
  }
}

module.exports = new ZTCrawler()
```

### 7.5 模块统一导出

#### 7.5.1 crawlers/index.js

```javascript
const sf = require('./sf')
const zt = require('./zt')
const yt = require('./yt')
const yd = require('./yd')
const st = require('./st')
const jt = require('./jt')
const ems = require('./ems')
const db = require('./db')

module.exports = {
  sf,
  zt,
  yt,
  yd,
  st,
  jt,
  ems,
  db
}
```

---

## OpenClaw集成设计

### 8.1 OpenClaw工具封装

#### 8.1.1 utils/openclaw.js

```javascript
const puppeteer = require('puppeteer')
const logger = require('./logger')

class OpenClaw {
  constructor() {
    this.browser = null
    this.pagePool = []
    this.maxPages = 5
    this.initPromise = null
  }
  
  async init() {
    if (this.initPromise) {
      return this.initPromise
    }
    
    this.initPromise = this._initBrowser()
    return this.initPromise
  }
  
  async _initBrowser() {
    this.browser = await puppeteer.launch({
      headless: 'new',
      args: [
        '--no-sandbox',
        '--disable-setuid-sandbox',
        '--disable-dev-shm-usage',
        '--disable-accelerated-2d-canvas',
        '--disable-gpu'
      ]
    })
    
    logger.info('OpenClaw 浏览器初始化完成')
  }
  
  async fetch(options) {
    await this.init()
    
    const { url, method = 'GET', data, headers, timeout = 10000, waitFor } = options
    
    let page = null
    
    try {
      page = await this.browser.newPage()
      
      await page.setDefaultTimeout(timeout)
      await page.setRequestInterception(true)
      
      page.on('request', (request) => {
        const resourceType = request.resourceType()
        if (['image', 'stylesheet', 'font'].includes(resourceType)) {
          request.abort()
        } else {
          request.continue()
        }
      })
      
      if (headers) {
        await page.setExtraHTTPHeaders(headers)
      }
      
      let response
      
      if (method === 'GET') {
        response = await page.goto(url, {
          waitUntil: 'networkidle2',
          timeout
        })
      } else {
        await page.goto(url, { waitUntil: 'domcontentloaded', timeout })
        
        if (data) {
          await page.evaluate((postData) => {
            // 根据具体页面实现POST逻辑
          }, data)
        }
      }
      
      if (waitFor) {
        await page.waitForSelector(waitFor, { timeout })
      }
      
      const result = await page.evaluate(() => {
        // 根据具体页面解析数据
        return {}
      })
      
      return result
      
    } catch (error) {
      logger.error('OpenClaw fetch error', { url, error: error.message })
      throw error
    } finally {
      if (page) {
        await page.close()
      }
    }
  }
  
  async close() {
    if (this.browser) {
      await this.browser.close()
      this.browser = null
      logger.info('OpenClaw 浏览器已关闭')
    }
  }
}

module.exports = {
  openclaw: new OpenClaw()
}
```

### 8.2 错误处理机制

```javascript
class OpenClawError extends Error {
  constructor(message, code, originalError) {
    super(message)
    this.code = code
    this.originalError = originalError
  }
}

const ERROR_CODES = {
  TIMEOUT: 'TIMEOUT',
  NETWORK_ERROR: 'NETWORK_ERROR',
  PARSE_ERROR: 'PARSE_ERROR',
  BLOCKED: 'BLOCKED',
  UNKNOWN: 'UNKNOWN'
}

async function fetchWithErrorHandling(options) {
  try {
    const result = await openclaw.fetch(options)
    return { success: true, data: result }
  } catch (error) {
    let code = ERROR_CODES.UNKNOWN
    
    if (error.name === 'TimeoutError') {
      code = ERROR_CODES.TIMEOUT
    } else if (error.message.includes('net::')) {
      code = ERROR_CODES.NETWORK_ERROR
    }
    
    logger.error('OpenClaw错误', {
      code,
      message: error.message,
      url: options.url
    })
    
    return { 
      success: false, 
      error: new OpenClawError(error.message, code, error)
    }
  }
}
```

### 8.3 重试机制

```javascript
async function fetchWithRetry(options, maxRetries = 3) {
  let lastError
  
  for (let i = 0; i < maxRetries; i++) {
    try {
      const result = await fetchWithErrorHandling(options)
      if (result.success) {
        return result.data
      }
      lastError = result.error
    } catch (error) {
      lastError = error
    }
    
    if (i < maxRetries - 1) {
      await sleep(1000 * (i + 1))
    }
  }
  
  throw lastError
}

function sleep(ms) {
  return new Promise(resolve => setTimeout(resolve, ms))
}
```

---

## 数据库设计

### 9.1 缓存数据结构（Redis/内存缓存）

```
Key格式: price:{senderProvince}:{senderCity}:{receiverProvince}:{receiverCity}:{weight}
Value: JSON格式的价格结果
TTL: 300秒（5分钟）
```

### 9.2 查询日志表（可选扩展）

```sql
CREATE TABLE query_logs (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  query_id VARCHAR(50) NOT NULL,
  sender_province VARCHAR(50),
  sender_city VARCHAR(50),
  receiver_province VARCHAR(50),
  receiver_city VARCHAR(50),
  weight DECIMAL(10, 2),
  result_count INT,
  lowest_price DECIMAL(10, 2),
  query_time DATETIME,
  ip_address VARCHAR(50),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_query_time (query_time),
  INDEX idx_query_id (query_id)
);
```

---

## 错误处理与日志

### 10.1 全局错误处理中间件

```javascript
function errorHandler(err, req, res, next) {
  logger.error('全局错误', {
    message: err.message,
    stack: err.stack,
    url: req.url,
    method: req.method,
    body: req.body
  })
  
  const statusCode = err.statusCode || 500
  const message = err.message || '服务器内部错误'
  
  res.status(statusCode).json({
    code: statusCode,
    message,
    data: null
  })
}
```

### 10.2 日志配置

```javascript
const winston = require('winston')
const path = require('path')

const logger = winston.createLogger({
  level: 'info',
  format: winston.format.combine(
    winston.format.timestamp({ format: 'YYYY-MM-DD HH:mm:ss' }),
    winston.format.errors({ stack: true }),
    winston.format.json()
  ),
  transports: [
    new winston.transports.Console({
      format: winston.format.combine(
        winston.format.colorize(),
        winston.format.printf(({ level, message, timestamp }) => {
          return `${timestamp} [${level}]: ${message}`
        })
      )
    }),
    new winston.transports.File({
      filename: path.join(__dirname, '../../logs/error.log'),
      level: 'error'
    }),
    new winston.transports.File({
      filename: path.join(__dirname, '../../logs/combined.log')
    })
  ]
})

module.exports = logger
```

---

## 性能优化策略

### 11.1 并发控制

```javascript
const { default: PQueue } = require('p-queue')

const queue = new PQueue({
  concurrency: 5,
  interval: 1000,
  intervalCap: 5
})

async function fetchWithQueue(crawler, params) {
  return queue.add(() => crawler.getPrice(params))
}
```

### 11.2 缓存策略

- **短期缓存**：相同查询条件5分钟内返回缓存结果
- **缓存预热**：热门路线价格预加载
- **缓存失效**：定时更新缓存数据

### 11.3 超时控制

```javascript
const DEFAULT_TIMEOUT = 10000
const MAX_TIMEOUT = 30000

async function fetchWithTimeout(promise, ms) {
  return Promise.race([
    promise,
    new Promise((_, reject) => 
      setTimeout(() => reject(new Error('Timeout')), ms)
    )
  ])
}
```

---

## 安全设计

### 12.1 接口限流

```javascript
const rateLimit = require('express-rate-limit')

const limiter = rateLimit({
  windowMs: 60 * 1000,
  max: 10,
  message: {
    code: 429,
    message: '请求过于频繁，请稍后再试',
    data: null
  }
})

app.use('/api/', limiter)
```

### 12.2 参数验证

```javascript
const Joi = require('joi')

const compareSchema = Joi.object({
  senderProvince: Joi.string().required(),
  senderCity: Joi.string().required(),
  senderDistrict: Joi.string().allow(''),
  receiverProvince: Joi.string().required(),
  receiverCity: Joi.string().required(),
  receiverDistrict: Joi.string().allow(''),
  weight: Joi.number().min(0.1).max(100).required()
})

function validateCompare(req, res, next) {
  const { error } = compareSchema.validate(req.body)
  if (error) {
    return res.status(400).json({
      code: 400,
      message: error.details[0].message,
      data: null
    })
  }
  next()
}
```

### 12.3 安全头

```javascript
const helmet = require('helmet')

app.use(helmet())
app.use(cors({
  origin: process.env.ALLOWED_ORIGINS?.split(',') || '*'
}))
```

---

## 测试方案

### 13.1 单元测试

```javascript
const { expect } = require('chai')
const sinon = require('sinon')
const courierService = require('../services/courierService')

describe('CourierService', () => {
  describe('comparePrices', () => {
    it('应该返回按价格排序的结果', async () => {
      const params = {
        senderProvince: '浙江省',
        senderCity: '杭州市',
        receiverProvince: '北京市',
        receiverCity: '北京市',
        weight: 1
      }
      
      const result = await courierService.comparePrices(params)
      
      expect(result.results).to.be.an('array')
      expect(result.results.length).to.be.at.least(3)
      
      for (let i = 1; i < result.results.length; i++) {
        expect(result.results[i].price)
          .to.be.at.least(result.results[i-1].price)
      }
      
      expect(result.results[0].isLowest).to.be.true
    })
  })
})
```

### 13.2 集成测试

```javascript
const request = require('supertest')
const app = require('../app')

describe('POST /api/compare', () => {
  it('应该返回比价结果', async () => {
    const response = await request(app)
      .post('/api/compare')
      .send({
        senderProvince: '浙江省',
        senderCity: '杭州市',
        receiverProvince: '北京市',
        receiverCity: '北京市',
        weight: 1
      })
      .expect(200)
    
    expect(response.body.code).to.equal(200)
    expect(response.body.data.results).to.be.an('array')
  })
  
  it('参数错误应该返回400', async () => {
    const response = await request(app)
      .post('/api/compare')
      .send({
        weight: -1
      })
      .expect(400)
    
    expect(response.body.code).to.equal(400)
  })
})
```

### 13.3 验收标准

| 测试项 | 预期结果 |
|-------|---------|
| 至少3家快递可用 | 成功返回≥3家快递价格 |
| 返回结果稳定 | 同一查询条件多次请求结果一致 |
| 最低价排序正确 | 结果数组第一个元素isLowest为true |
| 价格升序排列 | results数组按price升序排列 |
| 超时处理 | 单个快递超时不影响其他快递结果 |
| 错误处理 | 参数错误返回400，服务器错误返回500 |

---

## 部署方案

### 14.1 开发环境

```bash
# 后端
cd backend
pnpm install
pnpm dev

# 前端
cd frontend
pnpm install
pnpm dev
```

### 14.2 生产环境

```yaml
version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always
    
  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always
```

### 14.3 PM2配置

```javascript
module.exports = {
  apps: [{
    name: 'kuaibi-backend',
    script: './src/app.js',
    instances: 2,
    exec_mode: 'cluster',
    env_production: {
      NODE_ENV: 'production',
      PORT: 3000
    }
  }]
}
```

---

## 开发计划

### 15.1 开发阶段

| 阶段 | 任务 | 预计时间 |
|-----|------|---------|
| 第一阶段 | 项目初始化、基础架构搭建 | 1天 |
| 第二阶段 | 后端API开发、快递模块开发 | 3天 |
| 第三阶段 | OpenClaw集成、爬虫实现 | 2天 |
| 第四阶段 | 前端页面开发 | 2天 |
| 第五阶段 | 联调测试、Bug修复 | 1天 |
| 第六阶段 | 性能优化、部署上线 | 1天 |

### 15.2 开发优先级

1. **P0（必须）**
   - 后端/compare接口
   - 至少3家快递爬虫模块
   - 前端查询表单和结果展示
   - 基础错误处理

2. **P1（重要）**
   - 缓存机制
   - 接口限流
   - 日志系统
   - 参数验证

3. **P2（可选）**
   - 查询历史记录
   - 更多快递公司支持
   - 价格趋势分析
   - 用户收藏功能

---

## 附录

### A. 快递公司编码对照表

| 编码 | 名称 | 官网 |
|-----|------|------|
| sf | 顺丰速运 | https://www.sf-express.com |
| zt | 中通快递 | https://www.zto.com |
| yt | 圆通速递 | https://www.yto.net.cn |
| yd | 韵达快递 | https://www.yundaex.com |
| st | 申通快递 | https://www.sto.cn |
| jt | 极兔速递 | https://www.jtexpress.cn |
| ems | 邮政EMS | https://www.ems.com.cn |
| db | 德邦快递 | https://www.deppon.com |

### B. 环境变量配置

```bash
# 后端 .env
NODE_ENV=development
PORT=3000
LOG_LEVEL=debug

# 前端 .env.development
VITE_API_BASE_URL=http://localhost:3000/api
```

### C. Git提交规范

```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式调整
refactor: 重构
test: 测试相关
chore: 构建/工具相关
```

---

**文档版本**: v1.0  
**创建日期**: 2024-01-01  
**最后更新**: 2024-01-01  
**维护者**: 开发团队
